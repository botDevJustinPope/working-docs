import { CircularProgress } from './circular-progressmodel';

describe('CircularProgress', () => {
  it('should create an instance', () => {
    expect(new CircularProgress()).toBeTruthy();
  });
});
