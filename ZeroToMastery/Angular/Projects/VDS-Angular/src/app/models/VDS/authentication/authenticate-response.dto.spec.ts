import { AuthenticateResponseDTO } from './authenticate-response.dto';

describe('AuthenticateResponseDTO', () => {
  it('should create an instance', () => {
    expect(new AuthenticateResponseDTO()).toBeTruthy();
  });
});
